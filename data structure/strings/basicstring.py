# Sequence of characters inside quotes that can be accessed using 0 to n-1 or -n to -1 index.
# String in python are immutable, once created can't be changed only manupulated using slicing, formating, concatination

# String slicing : str[start:end+1:skip]
# Deleteing string using del keyword
# common function : upper(), lower(), capitalize(), swapcase(), title(), split(), strip(), startswith(),endswith(), replace(), find(), len(), join()